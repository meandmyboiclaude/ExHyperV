<Page xmlns:properties="clr-namespace:ExHyperV.Properties"
    x:Class="ExHyperV.Views.Pages.USBPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ExHyperV.ViewModels"
    xmlns:converters="clr-namespace:ExHyperV.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance vm:USBPageViewModel, IsDesignTimeCreatable=True}"
    mc:Ignorable="d" Title="USBPage">

    <Page.DataContext>
        <vm:USBPageViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:CommandParameterConverter x:Key="CommandParameterConverter"/>
    </Page.Resources>

    <Grid>
        <StackPanel Margin="10,0,10,10">

            <ui:Card Margin="0,0,0,4" Padding="12" Background="{ui:ThemeResource SystemFillColorCautionBackgroundBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 左侧图标 -->
                    <ui:SymbolIcon Grid.Column="0" Symbol="Info24" FontSize="20" Margin="0,0,12,0" Foreground="{ui:ThemeResource SystemFillColorCautionBrush}"/>

                    <!-- 中间文字内容：使用 Run 让标题和正文同排 -->
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" TextWrapping="Wrap">
            <Run Text="{x:Static properties:Resources.USBPage_DriverNoticeDesc}" 
                 FontSize="16" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                    </TextBlock>

                    <!-- 右侧操作按钮 -->
                    <ui:Button Grid.Column="2" 
                   Content="{x:Static properties:Resources.USBPage_ViewGuide}" 
                   Appearance="Secondary"
                   Icon="{ui:SymbolIcon Link24}"
                   VerticalAlignment="Center"
                   Margin="12,0,0,0"
                   Command="{Binding OpenUrlCommand}"
                   CommandParameter="https://github.com/Justsenger/ExHyperV/issues/123" />
                </Grid>
            </ui:Card>
            
            <!-- Header -->
            <Grid Margin="0,5,0,0">
                <ui:Card BorderThickness="1" Padding="10">
                    <Grid>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:FontIcon Glyph="&#xE88E;" FontSize="24" FontFamily="{DynamicResource SegoeFluentIcons}"/>
                            <TextBlock Text="{x:Static properties:Resources.USBPage_AssignableDevices}" FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                        <ui:Button HorizontalAlignment="Right"
                                   Content="{x:Static properties:Resources.USBPage_Refresh}" 
                                   Command="{Binding LoadDataCommand}"
                                   IsEnabled="{Binding IsUiEnabled}"/>
                    </Grid>
                </ui:Card>
            </Grid>

            <!-- Device List -->
            <ItemsControl ItemsSource="{Binding Devices}" Margin="0,0,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Margin="0,5,0,0" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <ui:FontIcon Grid.Column="0" 
                                 Glyph="&#xE88E;" 
                                 FontSize="24" 
                                 FontFamily="{DynamicResource SegoeFluentIcons}" 
                                 Margin="0,0,15,0"/>

                                <TextBlock Grid.Column="1" VerticalAlignment="Center" TextTrimming="CharacterEllipsis">
<Run Text="{Binding Description}" FontSize="16"/>
<Run Text="  " FontSize="12"/>
<Run Text="Bus:" FontSize="11" Foreground="Gray"/>
<Run Text="{Binding BusId, Mode=OneWay}" FontSize="11" Foreground="Gray"/>
<Run Text="  ID:" FontSize="11" Foreground="Gray"/>
<Run Text="{Binding VidPid, Mode=OneWay}" FontSize="11" Foreground="Gray"/>
                                </TextBlock>

                                <ui:DropDownButton Grid.Column="2" 
                                       Content="{Binding CurrentAssignment}" 
                                       Margin="10,0,0,0"
                                       IsEnabled="{Binding DataContext.IsUiEnabled, RelativeSource={RelativeSource AncestorType=Page}}">
                                    <ui:DropDownButton.Flyout>
                                        <ContextMenu ItemsSource="{Binding AssignmentOptions}">
                                            <ContextMenu.ItemContainerStyle>
                                                <Style TargetType="MenuItem">
                                                    <Setter Property="Header" Value="{Binding}"/>
                                                    <Setter Property="Command" Value="{Binding DataContext.ChangeAssignmentCommand, RelativeSource={RelativeSource AncestorType=Page}}"/>
                                                    <Setter Property="CommandParameter">
                                                        <Setter.Value>
                                                            <MultiBinding Converter="{StaticResource CommandParameterConverter}">
                                                                <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType=ui:Card}"/>
                                                                <Binding/>
                                                            </MultiBinding>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ContextMenu.ItemContainerStyle>
                                        </ContextMenu>
                                    </ui:DropDownButton.Flyout>
                                </ui:DropDownButton>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <ProgressBar Panel.ZIndex="1" VerticalAlignment="Top" Height="4"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</Page>