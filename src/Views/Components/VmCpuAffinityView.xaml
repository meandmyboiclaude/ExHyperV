<UserControl xmlns:properties="clr-namespace:ExHyperV.Properties" x:Class="ExHyperV.Views.Components.VmCpuAffinityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:ExHyperV.ViewModels"
             xmlns:converters="clr-namespace:ExHyperV.Converters"
             xmlns:tools="clr-namespace:ExHyperV.Tools" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="SelectableCoreBoxStyle" TargetType="CheckBox">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <!-- 重要：StretchUniformGrid 会参考这个最小高度来决定行高 -->
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="RootBorder" 
                                CornerRadius="4" 
                                BorderThickness="1" 
                                Background="#08FFFFFF"
                                BorderBrush="#1A888888">
                            <Grid>
                                <TextBlock Text="{Binding CoreId}"
                                           FontSize="16" 
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                                           Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>

                                <TextBlock FontSize="12" Margin="5"
                                           HorizontalAlignment="Left" VerticalAlignment="Top" 
                                           FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CoreType}" Value="Performance">
                                                    <Setter Property="Text" Value="⚡"/>
                                                    <Setter Property="Foreground" Value="#FFFAA146"/>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CoreType}" Value="Efficient">
                                                    <Setter Property="Text" Value="🍃"/>
                                                    <Setter Property="Foreground" Value="#FF107C10"/>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="#FF54C7FC"/>
                                <Setter TargetName="RootBorder" Property="Background" Value="#33117DBB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Margin="24,16,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{x:Static properties:Resources.Label_CpuAffinity}" FontSize="20" FontWeight="Regular" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                <TextBlock Text="{x:Static properties:Resources.Label_SelectCores}" FontSize="12" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <ui:Button Grid.Column="1" Appearance="Transparent" Padding="8" VerticalAlignment="Top" Command="{Binding GoToCpuSettingsCommand}">
                <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="18"/>
            </ui:Button>
        </Grid>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" VerticalAlignment="Top" Height="2" Margin="0,-2,0,0"
                     Visibility="{Binding IsLoadingSettings, Converter={StaticResource BoolToVis}}"/>

        <!-- 核心显示区域 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Border Padding="24,0,24,0">
                <ItemsControl x:Name="CoresItemsControl"
                              ItemsSource="{Binding AffinityHostCores}"
                              Background="Transparent"
                              PreviewMouseLeftButtonDown="ItemsControl_PreviewMouseLeftButtonDown"
                              PreviewMouseMove="ItemsControl_PreviewMouseMove"
                              PreviewMouseLeftButtonUp="ItemsControl_PreviewMouseLeftButtonUp">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <!-- 【已修改】使用自定义的 StretchUniformGrid 替换原生 UniformGrid -->
                            <tools:StretchUniformGrid Columns="{Binding AffinityColumns}" 
                                                      Rows="{Binding AffinityRows}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                      Style="{StaticResource SelectableCoreBoxStyle}"
                                      IsHitTestVisible="False"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </ScrollViewer>

        <!-- 底部操作栏 -->
        <Border Grid.Row="3" Padding="24,16,24,24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="{x:Static properties:Resources.Button_Apply}" Appearance="Primary" Icon="Checkmark24"
                           Command="{Binding SaveAffinityCommand}" Width="80"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>