<UserControl x:Class="ExHyperV.Views.Components.VmGPUSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:properties="clr-namespace:ExHyperV.Properties"
             xmlns:converters="clr-namespace:ExHyperV.Converters"
             xmlns:vm="clr-namespace:ExHyperV.ViewModels"
             xmlns:models="clr-namespace:ExHyperV.Models"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="1000"
             UseLayoutRounding="True"
             d:DataContext="{d:DesignInstance vm:VirtualMachinesPageViewModel}">

    <UserControl.Resources>
        <converters:EqualityToVisibilityConverter x:Key="EqualityToVisibilityConverter" />
        <converters:GpuImagePathConverter x:Key="GpuImagePathConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. 顶部标题栏：下边距从 24 减小到 8，使卡片上移 -->
        <Grid Grid.Row="0" Margin="24,16,24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:FontIcon Glyph="&#xF211;" FontFamily="{DynamicResource SegoeFluentIcons}" FontSize="24" Margin="0,0,12,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Static properties:Resources.Label_GpuSettings}" FontSize="20" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ui:Button Appearance="Primary" Margin="0,0,8,0"
                   Padding="16,7"
                   Command="{Binding GoToAddGpuCommand}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Add24" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="{x:Static properties:Resources.Button_Add}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                    <ui:Button Appearance="Transparent" Padding="7,7" Command="{Binding GoBackCommand}">
                        <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="18"/>
                    </ui:Button>
                </StackPanel>
            </Grid>


        <!-- 2. 显卡列表区域 -->
        <Grid Grid.Row="1" Margin="24,0,24,24">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding SelectedVm.AssignedGpus}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:VmGpuAssignment}">
                                <ui:CardExpander Margin="0,5,0,0" Padding="8" IsExpanded="True">
                                    <ui:CardExpander.Header>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- 图标匹配 -->
                                            <Image Height="64" Width="64" Margin="4,2,10,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality">
                                                <Image.Source>
                                                    <MultiBinding Converter="{StaticResource GpuImagePathConverter}">
                                                        <Binding Path="Manu"/>
                                                        <Binding Path="Name"/>
                                                    </MultiBinding>
                                                </Image.Source>
                                            </Image>

                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="16" VerticalAlignment="Center"/>

                                            <!-- ✅ 卸载按钮：增加右边距 Margin="0,0,28,0" 以避开右侧自带的展开箭头图标 -->
                                            <ui:Button Grid.Column="2" 
                                                       Content="{x:Static properties:Resources.Button_Uninstall}" 
                                                       Appearance="Secondary" 
                                                       Padding="16,6"
                                                       Margin="0,0,28,0"
                                                       Command="{Binding DataContext.RemoveGpuCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding AdapterId}"/>
                                        </Grid>
                                    </ui:CardExpander.Header>

                                    <!-- 内容布局：间距50，行高32，字号16 -->
                                    <Grid Margin="50,0,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" MinWidth="125"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="32"/>
                                            <RowDefinition Height="32"/>
                                            <RowDefinition Height="32"/>
                                            <RowDefinition Height="32"/>
                                            <RowDefinition Height="32"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{x:Static properties:Resources.Label_Manu}" FontSize="16" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Vendor}" FontSize="16" VerticalAlignment="Center"/>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{x:Static properties:Resources.Label_Ram}" FontSize="16" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding RamDisplay}" FontSize="16" VerticalAlignment="Center"/>

                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{x:Static properties:Resources.Label_DriverVer}" FontSize="16" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DriverVersion}" FontSize="16" VerticalAlignment="Center"/>
                                        
                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{x:Static properties:Resources.Label_GpuPartitionId}" FontSize="16" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding AdapterId}" FontSize="16" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                        <TextBlock Grid.Row="4" Grid.Column="0" Text="{x:Static properties:Resources.Label_GpuPartitionPath}" FontSize="16" VerticalAlignment="Center" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding PName}" FontSize="16" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>


                                    </Grid>
                                </ui:CardExpander>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>